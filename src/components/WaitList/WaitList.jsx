import React from 'react';

const WaitList = () => {
    const { products, setProducts, wishlist } = useOutletContext();
    return (
        <div>
            
        </div>
    );
};

export default WaitList;